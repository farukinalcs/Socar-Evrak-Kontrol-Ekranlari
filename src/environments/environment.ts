// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  appVersion: 'v712ReportStoreTheme',
  USERDATA_KEY: 'authf649fc9a5f55',
  isMockEnabled: true,
  //apiUrl:'http://10.20.27.214:8070/api',
  //apiUrl8086: 'https://api.meyerangel.com:8086/api',
  //apiUrl: 'https://api.meyerangel.com:1879/api',
  // apiUrl: 'https://api.meyerangel.com:8086/api',
  apiUrl: 'http://localhost:8080/api',
  //apiUrl: 'https://kioskbilgiapi.socar.com.tr/api',
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
